apiVersion: v1
kind: Service
metadata:
  name: host
  namespace: nowoptics
spec:
  selector:
    app: host
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: home
  namespace: nowoptics
spec:
  selector:
    app: home
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3001
  type: ClusterIP 