import { render } from "@testing-library/react";
import AcceptInsurancePlan from "../AcceptInsurancePlan";

describe("Accept InsurancePlan component", () => {
  it("renders the Accept InsurancePlan component", () => {
    render(<AcceptInsurancePlan />);
  });
});
