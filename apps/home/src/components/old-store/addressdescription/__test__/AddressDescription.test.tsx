import { render } from "@testing-library/react";
import AddressDescription from "../AddressDescription";

describe("Address Description component", () => {
  it("renders the Address Description component", () => {
    render(<AddressDescription />);
  });
});
