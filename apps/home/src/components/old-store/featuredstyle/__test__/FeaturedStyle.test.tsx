import { render } from "@testing-library/react";
import FeaturedStyle from "../FeaturedStyle";

describe("Featured Style component", () => {
  it("renders the Featured Style component", () => {
    render(<FeaturedStyle />);
  });
});
