import { render } from "@testing-library/react";
import TwoPairs from "../TwoPairs";

describe("Two Pairs component", () => {
  it("renders the Two Pairs component", () => {
    render(<TwoPairs />);
  });
});
