import { render } from "@testing-library/react";
import StoreOffer from "../StoreOffer";

describe("Store Offer component", () => {
  it("renders the Store Offer component", () => {
    render(<StoreOffer />);
  });
});
