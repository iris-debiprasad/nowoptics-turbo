import { render } from "@testing-library/react";
import HeroBannerRxRenewal from "../hero-banner";

describe("Hero Banener component", () => {
  it("renders the Hero Banner section component", () => {
    render(<HeroBannerRxRenewal />);
  });
});
