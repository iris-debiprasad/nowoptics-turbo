import { render } from "@testing-library/react"
import BackdropLoader from "../BackdropLoader"

describe("", () => {

    it("render", () => {
        (render(<BackdropLoader openLoader={true} />))
    })
})